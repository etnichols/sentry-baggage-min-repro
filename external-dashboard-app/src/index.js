import App from './pages/App';
import React from 'react';
// store and app container
import { render } from 'react-dom';

// // init the data store
// const store = window.store = new Store();

// render the app
render(<App />, document.getElementById('root-1'));
